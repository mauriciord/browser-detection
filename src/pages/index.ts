import { lazy } from "react";

export const BrowserVerification = lazy(() => import("./BrowserVerification"));

export const Dashboard = lazy(() => import("./Dashboard"));

export const NoMatch = lazy(() => import("./NoMatch"));
